# AI 智能客服系统 - 全面测试审查报告

> 审查时间：2026-08-28 14:09
> 审查范围：后端 api-customer-backend + 前端 ai-customer-frontend + 安全配置 + 基础设施

---

## 一、项目现状总览

| 维度 | 状态 | 说明 |
|------|------|------|
| 后端服务 | ✅ 运行中 | FastAPI, 8000 端口, 48 个 API 端点 |
| 前端服务 | ✅ 运行中 | Vite + Vue3 + TypeScript, 5176 端口 |
| Redis | ✅ 运行中 | Docker 独立容器, 端口 6380 |
| MinIO | ✅ 运行中 | Docker 容器, `/minio/health/ready` OK |
| Milvus | ✅ 运行中 | 集合 `knowledge_embeddings` 正常 |
| MySQL | ⚠️ 未使用 | 容器运行但 `.env` 配置为 `DB_TYPE=sqlite` |
| LLM/Embedding | ✅ 真实调用 | DeepSeek API Key 已配置 |
| 数据库 | ✅ SQLite | `data/ai_customer.db` |

**代码规模：** 后端服务层 4088 行 Python，前端 9 个 API 模块 + Vue3 + Pinia + Element Plus。

---

## 二、已修复问题回顾（2026-08-27）

| 编号 | 问题 | 状态 |
|------|------|------|
| P0-1 | MySQL/Milvus 非原子提交导致索引状态不一致 | ✅ 已修复（先 Milvus 后 MySQL） |
| P0-2 | 全系统处于 mock 模式，RAG 检索无实际效果 | ✅ 已修复（配置 DeepSeek Key） |
| P1-1 | Embedding 串行批次性能差 | ✅ 已修复（asyncio.gather 并发） |
| P1-2 | 大文件内存压力 | ✅ 已修复（50MB 限制 + 200页限制） |
| P1-3 | Redis 缓存无效无超时 | ✅ 已修复（SCAN 次数上限 50） |

---

## 三、当前问题清单（按优先级）

### 🔴 高危（P0 - 必须立即修复）

#### P0-1：JWT_SECRET_KEY 仍为开发占位符
- **位置：** `.env` 第 47 行
- **影响：** 当前开发环境正常；若误部署到生产，JWT 签名可被任何人预测
- **修复方案：**
```powershell
# 生成新的 JWT_SECRET_KEY
$chars = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*-_+=~'
$password = -join ((1..64) | ForEach-Object { $chars[(Get-Random -Maximum $chars.Length)] })
echo $password
```
- **操作：** 替换 `.env` 第 47 行的 `please-change-this-secret-key-to-random-string-at-least-32-chars`

#### P0-2：CORS 配置为 `*` + `allow_credentials=True`（生产安全漏洞）
- **位置：** `.env` 第 5 行 + `main.py` 第 218 行
- **影响：** 任何网站可向你的 API 发送携带凭证的请求（CSRF + Cookie 窃取）
- **修复方案：** 生产环境必须显式配置 `CORS_ALLOWED_ORIGINS=https://your-domain.com`

#### P0-3：MinIO 凭证明文存储（含公网泄露风险）
- **位置：** `.env` 第 29-30 行
- **影响：** 若 `.env` 被提交到 Git，凭证泄露
- **修复方案：** 将 `.env` 加入 `.gitignore`，生产环境使用环境变量注入

---

### 🟠 中危（P1 - 建议尽快修复）

#### P1-1：API 测试文件 `api-test-results.json` 异常
- **位置：** 项目根目录
- **影响：** 测试脚本输出异常（response body 为 `undefined`）
- **分析：** 后端登录接口实际返回正常（手动 curl 验证通过），前端测试脚本可能有 JSON 解析问题
- **修复方案：** 检查 `api_test.py` 的响应解析逻辑，或重新运行测试

#### P1-2：MySQL 容器存在但未使用（资源浪费 + 配置混淆）
- **位置：** `.env` 第 8 行 `DB_TYPE=sqlite`
- **影响：** 浪费 Docker 资源（MySQL 容器运行中），后续迁移到 MySQL 需额外工作
- **建议：** 确认是否使用 MySQL；若开发环境仅需 SQLite，可从 `docker-compose.yml` 移除 MySQL 配置

#### P1-3：Redis 无密码（端口 6380 未配置 REDIS_PASSWORD）
- **位置：** `.env` 第 19 行 `REDIS_PASSWORD=`
- **影响：** 若 Redis 暴露到公网，可被直接访问（无认证）
- **修复方案：**
```powershell
# 生成 Redis 密码
$password = -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 20 | ForEach-Object {[char]$_})
echo $password
```
- **操作：** 更新 `.env` 第 19 行 + `docker-compose.yml` Redis 容器配置

#### P1-4：OPENAI_COMPAT_API_KEY 为占位符
- **位置：** `.env` 第 71 行
- **影响：** OpenAI 兼容端点 `/api/v1/openai/*` 可能被未授权访问
- **修复方案：** 生成随机密钥替换

#### P1-5：WEBHOOK_HMAC_SECRET 为占位符
- **位置：** `.env` 第 67 行
- **影响：** Webhook 签名验证形同虚设，攻击者可伪造 webhook 请求
- **修复方案：** 生成随机密钥替换

---

### 🟡 低危（P2 - 建议优化）

#### P2-1：缺少 LLM 调用指标监控
- **位置：** `llm_service.py`
- **建议：** 集成 Prometheus 指标（调用次数、延迟、错误率、降级率）

#### P2-2：Mock 回复硬编码
- **位置：** `llm_service.py` 第 125-134 行
- **建议：** 改为可配置模板，支持按意图分类

#### P2-3：PDF 解析无页数限制
- **位置：** `document_parser.py`
- **建议：** 增加 200 页限制（技术分析报告已建议，P1-2 修复中）

#### P2-4：流式超时日志缺少 model 信息
- **位置：** `llm_service.py` 第 465 行附近
- **建议：** 增加 model_name 和耗时上下文

#### P2-5：前端 API 测试显示 `undefined`
- **位置：** `api-test-results.json`
- **分析：** 手动 curl 验证后端登录接口正常，可能是测试脚本解析问题

---

## 四、安全审计结果

| 安全项 | 状态 | 说明 |
|--------|------|------|
| JWT 认证 | ✅ 正常 | 中间件实现，支持黑名单 |
| CSRF 防护 | ✅ 正常 | 写请求 Origin/Referer 校验 |
| SSRF 防护 | ✅ 正常 | URL 安全校验，封锁内网 IP |
| 登录暴力破解防护 | ✅ 正常 | 5 次/分钟 + IP 封禁 30 分钟 |
| 密码哈希 | ✅ 正常 | bcrypt + SHA256 预哈希 |
| 限流 | ✅ 正常 | Redis ZSet 滑动窗口 |
| 生产环境密钥校验 | ✅ 正常 | fail-fast 阻止默认密钥部署 |
| API 文档暴露 | ⚠️ 风险 | `/docs` 和 `/redoc` 公开访问（开发环境可接受） |
| OpenAI 兼容端点 | ⚠️ 风险 | 需配置独立 API Key |

---

## 五、测试结果

### 后端 API 连通性测试

| 接口 | 方法 | 状态 | 说明 |
|------|------|------|------|
| `/health` | GET | ✅ 200 | `{"status":"ok"}` |
| `/api/v1/auth/login` | POST | ✅ 200 | 登录正常，返回 JWT Token |
| `/api/v1/chat/sessions` | GET | ✅ 需 Token | 受保护接口 |

### 环境连通性测试

| 服务 | 地址 | 状态 |
|------|------|------|
| 后端 | http://localhost:8000 | ✅ OK |
| 前端 | http://localhost:5176 | ✅ OK |
| Redis | localhost:6380 | ✅ OK |
| MinIO | localhost:9000 | ✅ OK |
| Milvus | localhost:19530 | ✅ OK |
| SQLite | data/ai_customer.db | ✅ OK |

### Docker 容器状态

| 容器 | 状态 | 端口映射 |
|------|------|----------|
| ai-customer-redis | Up 4 hours | 0.0.0.0:6380→6379/tcp |
| ai-customer-milvus | Up 4 hours (healthy) | 19530, 9091 |
| ai-customer-minio | Up 4 hours (healthy) | 9000-9001 |
| ai-customer-etcd | Up 4 hours (healthy) | 2379 |

---

## 六、架构评估

### 优点
1. **分层清晰：** services → routers → middleware，依赖注入链完整
2. **降级优先：** Milvus/MinIO/Redis/LLM 全部有 mock 降级
3. **双写一致性：** Redis 先行，MySQL 异步
4. **幂等防重：** SessionMapper.get_or_create + message_id 内存缓存 TTL 5min
5. **独立 session 持久化：** ChatService finally 块使用 AsyncSessionLocal 避免主会话泄漏
6. **安全机制完善：** CSRF、SSRF、限流、暴力破解防护、生产密钥校验

### 待改进
1. **配置管理：** 开发/生产环境配置未分离（建议多 .env 文件）
2. **日志可观测性：** 缺少结构化日志和指标导出
3. **测试覆盖：** 缺少自动化测试（仅有 api_test.py 手动脚本）
4. **前端状态管理：** Pinia store 可进一步优化（目前仅 auth.ts 和 chat.ts）
5. **数据库迁移：** SQLite 开发环境，生产环境需迁移到 MySQL/PostgreSQL

---

## 七、修复优先级建议

```
立即修复（今天）：
  1. 替换 JWT_SECRET_KEY 为强随机密钥
  2. 替换 OPENAI_COMPAT_API_KEY
  3. 替换 WEBHOOK_HMAC_SECRET
  4. 检查前端 API 响应解析问题（api_test.py）

本周修复：
  5. 为 Redis 设置密码
  6. 确认 MySQL 使用策略（用则配置，不用则移除容器）
  7. 生产环境 CORS 配置检查

下月优化：
  8. 集成 Prometheus 监控
  9. PDF 页数限制（P2-3）
  10. TypeScript 严格模式检查
```

---

*审查完成于 2026-08-28 14:09*
*审查人：土豆 🥔*
