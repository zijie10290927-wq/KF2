# 京东接入配置与申请清单（AI 客服 Agent）

> 适用范围：`ai-customer-backend` 的 `JingdongAdapter`（`app/adapters/platforms/jd_adapter.py`）。
> 当前版本已打通**京东 IM 客服闭环**（Webhook 收消息 → RAG 回答 → 回推 → 转人工）。
> 本文档用于：**① 对齐现有配置项；② 给出开放平台申请流程；③ 列出后续业务接口（订单/物流/售后/咚咚）所需的扩展配置与权限。**

---

## 一、现有配置项总表（已实现）

所有配置均在 `app/config/settings.py` 定义，运行期从 `.env` 读取。`.env.example` 已包含模板。

| 配置项 | 类型 | 默认值 | 必填 | 说明 |
|---|---|---|---|---|
| `JD_API_BASE` | str | `https://api.jd.com/routerjson` | 否 | 京东 JOS（宙斯）网关地址。现有适配器**只走这一个网关**。 |
| `JD_APP_KEY` | str | 空 | **是**（生产） | 京东开放平台应用的 App Key，调用 JOS API 与 Webhook 验签共用。 |
| `JD_APP_SECRET` | str | 空 | **是**（生产） | 应用 App Secret，用于 JOS 签名（`secret+参数+secret` → MD5 大写）与 Webhook 验签兜底。 |
| `JD_WEBHOOK_SECRET` | str | 空 | 否 | Webhook 独立签名密钥。**留空时复用 `JD_APP_SECRET`**（适配器逻辑见 `verify_signature`）。若京东后台给独立 HMAC Secret 则填此处。 |
| `JD_IM_SEND_METHOD` | str | `jingdong.im.message.send` | 否 | 向买家推送 AI 回复的 JOS 方法名，可按京东业务版本调整。 |
| `JD_TRANSFER_HUMAN_METHOD` | str | `jingdong.im.waiter.transfer` | 否 | 转人工技能组的 JOS 方法名。 |

> **生产环境要求**：`JD_APP_KEY` / `JD_APP_SECRET` 二者缺任一，调 JOS 网关会抛 `AdapterSendError`；二者为空且 `APP_ENV=production` 时，Webhook 验签直接拒绝（`verify_signature` 返回 `False`）。开发环境允许降级放行。

---

## 二、`.env` 配置示例（可直接复制）

```dotenv
# 京东开放平台（JOS 协议）：Webhook 接收 + 消息推送 API
# 在 https://open.jd.com/ 开发者后台 → 应用管理 创建应用后获取
JD_API_BASE=https://api.jd.com/routerjson
JD_APP_KEY=your_jd_app_key
JD_APP_SECRET=your_jd_app_secret
# Webhook 签名密钥：留空时复用 JD_APP_SECRET；若京东后台提供独立 HMAC Secret 填在此处
JD_WEBHOOK_SECRET=
# 发消息 / 转人工方法名（如有京东业务版本差异，可按实际调整）
JD_IM_SEND_METHOD=jingdong.im.message.send
JD_TRANSFER_HUMAN_METHOD=jingdong.im.waiter.transfer
```

Webhook 接收端点（需在京东后台配置回调地址）：

```
POST {你的域名}/api/v1/webhook/jd        # 或 /api/v1/webhook/jingdong（别名）
```

该路由在 `app/adapters/webhook_router.py` 中按 `platform` 取适配器，`jingdong` / `jd` 均已注册到 `JingdongAdapter`（见 `app/adapters/platforms/__init__.py`）。

---

## 三、京东开放平台申请流程（清单）

要在生产环境真正调通，需按以下顺序完成开放平台侧配置。建议作为上线前 CheckList 逐项打勾。

### 步骤 1 — 注册开发者账号
- 入口：https://open.jd.com/ （京东商家开放平台 / 宙斯）。
- 用**商家主体**或**ISV 企业**资质注册开发者；个人主体通常无法申请客服/订单类敏感接口。

### 步骤 2 — 创建应用
- 路径：开发者后台 → **应用管理 → 创建应用**。
- 应用类型选择「**商家应用**」或「**ISV 服务市场应用**」（客服/订单/物流接口多要求商家自有应用或已授权的 ISV）。
- 创建后获得 **App Key** 与 **App Secret**（即 `JD_APP_KEY` / `JD_APP_SECRET`）。

### 步骤 3 — 申请 API 权限（关键）
- 在应用详情页的「**API 权限管理**」中勾选所需接口。不同接口归属不同"权限包"，需分别申请，部分需京东审核。
- 客服 IM / 订单 / 物流 / 售后 分属不同权限域，详见第四节《网关与权限点清单》。
- 审核周期：普通接口即时或 T+1；涉及用户隐私/交易数据的接口可能需 3–7 个工作日。

### 步骤 4 — 配置 Webhook 回调
- 在应用「**消息推送 / 事件订阅**」中填写回调 URL：`https://{你的域名}/api/v1/webhook/jd`。
- 设置**推送签名密钥**（若支持独立密钥，对应 `JD_WEBHOOK_SECRET`；否则留空复用 App Secret）。
- 京东推送常见两种形态：URL query 带 `app_key/timestamp/sign`，或 HTTP Header 带 `X-Jd-Sign`。现有适配器三种策略兼容（Header / Body 顶层 / Query 兜底）。

### 步骤 5 — 网关白名单 / IP 放行
- 若部署在固定出口 IP，将服务器出口 IP 加入京东「**IP 白名单**」，否则调用 JOS 网关会被拒。
- 若使用独立签名密钥，确保与 `JD_WEBHOOK_SECRET` 完全一致。

### 步骤 6 — 回填配置并验证
- 将 App Key / Secret / Webhook 密钥写入 `.env`，重启后端。
- 验证：`/health` 返回 `ok`；用京东测试号发一条 IM 消息，观察后端日志是否通过 `verify_signature` 并打印 `accepted`。

---

## 四、网关与权限点清单（按业务场景）

京东开放接口分布在**多个网关**，现有适配器仅覆盖 JOS 网关。下表用于申请权限与后续扩展时对照。

| 业务场景 | 接口 / 方法 | 网关 | 权限域 | 现状 |
|---|---|---|---|---|
| 收买家 IM 消息 | Webhook 推送 | 京东后台配置回调 | 消息推送/事件订阅 | ✅ 已实现 |
| 发消息给买家 | `jingdong.im.message.send` | `api.jd.com/routerjson` | IM 消息 | ✅ 已实现 |
| 转接人工 | `jingdong.im.waiter.transfer` | `api.jd.com/routerjson` | IM 客服 | ✅ 已实现 |
| 订单详情 | `jingdong.pop.order.get` | `api.jd.com/routerjson` | 订单查询 | ⬜ 待扩展 |
| 订单搜索 | `jingdong.pop.order.search` | `api.jd.com/routerjson` | 订单查询 | ⬜ 待扩展 |
| 物流轨迹订阅（推送） | `POST /ecap/v1/orders/trace/subscribe` | `open.jdl.com`（京东物流 JDL） | 物流轨迹 | ⬜ 待扩展 |
| 售后概要 | `GET /api/afterSaleNew/getAfsOutline` | `api-iop.jd.com`（京麦/IOP） | 售后服务 | ⬜ 待扩展 |
| 售后提交/审核 | `jingdong.pop.afs.*`（`afs/submit`、`afsOpenApprove`） | `api.jd.com/routerjson` 或 `api-iop.jd.com` | 售后服务 | ⬜ 待扩展 |
| 咚咚客服外链 | `GET /api/dongdong/shangjiadongdong`（返回 5 分钟有效 chatUrl） | `api-iop.jd.com` | 咚咚客服 | ⬜ 待扩展 |
| 小程序消息推送 | `jingdong.miniapp.dd.push`、发图 `jingdong.miniapp.pic.uploadImage` | `api.jd.com/routerjson` | 小程序 | ⬜ 待扩展 |
| 授权 Token 刷新 | 京麦/宙斯 OAuth token | 各网关 | 授权 | ⬜ 待扩展 |

> **网关说明**
> - **JOS 网关** `https://api.jd.com/routerjson`：请求体 `method=jingdong.xxx` + `360buy_param_json` + 标准 JOS 签名。现有 `_call_jos_api()` 仅打此网关。
> - **京麦 / IOP 网关** `https://api-iop.jd.com/api/...`：REST 风格路径，咚咚、售后概要等走这里，签名/鉴权形态与 JOS 不同。
> - **京东物流 JDL** `https://open.jdl.com/...`：物流轨迹订阅与查询，独立鉴权。

---

## 五、签名机制（JOS 标准，现有适配器已实现）

调用 `api.jd.com/routerjson` 需带签名，算法如下（适配器 `JingdongAdapter.jos_sign`）：

1. 收集除 `sign` 外的所有请求参数。
2. 按参数 **key 字母升序**排序。
3. 拼接：`secret + key1 + value1 + key2 + value2 + ... + secret`。
4. 对拼接串做 **MD5**，结果转**大写** → `sign`。

Webhook 验签同样基于该 Secret，并额外做时间戳防重放（±300 秒）。生产环境**必须**配置 `JD_APP_SECRET`，否则验签直接拒绝。

---

## 六、扩展集成所需的补充配置项（建议，尚未实现）

要将客服 Agent 从"只能对话"升级为"能查订单/物流/售后"，需在 `settings.py` 与 `.env` 增加以下配置项，并重构 `_call_jos_api()` 支持多网关：

| 建议配置项 | 说明 |
|---|---|
| `JD_IOP_BASE` | 京麦/IOP 网关 `https://api-iop.jd.com`，用于咚咚、售后概要。 |
| `JD_JDL_BASE` | 京东物流 JDL 网关 `https://open.jdl.com`。 |
| `JD_TOKEN` / `JD_TOKEN_EXPIRE` | 京麦/宙斯 OAuth 访问令牌及过期时间，用于 IOP/JDL 鉴权（与 JOS 的 AppKey/Secret 不同体系）。 |
| `JD_ORDER_METHOD` / `JD_LOGISTICS_METHOD` / `JD_AFTERSALE_METHOD` | 各业务查询的方法名/路径，便于按京东版本调整，保持与现有 `*_METHOD` 命名一致。 |
| `JD_CALLBACK_URL` | 物流轨迹订阅回推地址（JDL 主动推送轨迹时需填）。 |

> 架构提示：现有 `_call_jos_api(method, biz)` 写死了 `self.API_BASE`（`JD_API_BASE`）。扩展时应改为 `_call_jos_api(method, biz, base_url=...)`，按业务选择 JOS / IOP / JDL 网关，并分别处理各自鉴权头。这部分属于代码改造，不在本文档范围内；如需落地可另行安排。

---

## 七、生产环境注意事项

1. **密钥不出仓库**：`JD_APP_SECRET` / `JD_WEBHOOK_SECRET` 必须走环境变量或密钥管理，**禁止**提交进 `.env` 明文到 Git（`.env` 应被 `.gitignore` 忽略，仅提交 `.env.example`）。
2. **验签不可降级**：生产环境（`APP_ENV=production`）下 `JD_APP_SECRET` 缺失会拒绝所有 Webhook；切勿为"先跑通"而关闭验签。
3. **多网关隔离**：咚咚/售后/JDL 与 JOS 网关鉴权不同，扩展时分别申请、分别配置，避免混用密钥导致越权或拒签。
4. **频率与配额**：订单/物流/售后接口有 QPS 与日调用配额，需在开放平台后台关注，Agent 侧建议加缓存与限流。
5. **隐私合规**：买家订单、售后、物流属个人敏感信息，日志中脱敏，仅用于本轮会话答复，不得留存原文。

---

## 八、CheckList（上线前）

- [ ] 已在 open.jd.com 创建应用并获取 App Key / App Secret
- [ ] 已申请 IM 消息、订单查询、物流轨迹、售后服务等所需 API 权限（含审核通过）
- [ ] 已配置 Webhook 回调 URL 与签名密钥（对齐 `JD_WEBHOOK_SECRET`）
- [ ] 已将服务器出口 IP 加入京东 IP 白名单
- [ ] 已在 `.env` 填写 `JD_APP_KEY` / `JD_APP_SECRET` / `JD_WEBHOOK_SECRET`
- [ ] 后端 `/health` 返回 `ok`，测试 IM 消息可正常收/发/转人工
- [ ] （扩展项）已补充 `JD_IOP_BASE` / `JD_JDL_BASE` / `JD_TOKEN` 并改造多网关调用
- [ ] 密钥已通过环境变量/密钥管理注入，未落库明文
